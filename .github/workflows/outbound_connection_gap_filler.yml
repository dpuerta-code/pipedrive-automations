name: Outbound Connection Gap Filler

on:
  schedule:
    - cron: "0 12 * * *"   # Todos los dias 7am Colombia (UTC-5 -> 12:00 UTC)
  workflow_dispatch:
    inputs:
      test_mode:
        description: "true = solo probar con 5 leads, false = correr todo"
        required: true
        default: "true"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install requests

      - name: Ejecutar Outbound Connection Gap Filler
        env:
          PIPEDRIVE_API_TOKEN: ${{ secrets.PIPEDRIVE_API_TOKEN }}
          TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
        run: python outbound_connection_gap_filler.py

      - name: Guardar log de cambios
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: outbound-connection-gap-filler-result-log
          path: outbound_connection_gap_filler_result_log.json
          if-no-files-found: ignore
